[tool.ruff]
select = ["E", "W", "I", "N", "D", "F", "PT", "B", "ANN", "SIM"]
ignore = [ "ANN101" ]

[tool.pytest.ini_options]
markers = [
    "smoke: marks tests as smoke (deselect with '-m \"not smoke\"')",
    "regression: marks tests as regression (deselect with '-m \"not regression\"')",
]